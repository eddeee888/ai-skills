# PR skills

Skills related to working with pull requests — reviewing, describing, syncing,
or otherwise assisting with the PR lifecycle.

Add each skill as its own directory here, e.g. `pr-skills/<skill-name>/SKILL.md`.
